onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap1') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap1');
	event.player.tell(Text.lightPurple("You open up an Panda Blush Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap2') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap2');
	event.player.tell(Text.lightPurple("You open up an Hare Bounce Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap3') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap3');
	event.player.tell(Text.lightPurple("You open up an Kangaroo Logic Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap4') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap4');
	event.player.tell(Text.lightPurple("You open up an Fennec Shoot Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap5') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap5');
	event.player.tell(Text.lightPurple("You open up an Squirrel Talk Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap6') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap6');
	event.player.tell(Text.lightPurple("You open up an Butterfly Love Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap7') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap7');
	event.player.tell(Text.lightPurple("You open up an Lion Deal Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap8') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap8');
	event.player.tell(Text.lightPurple("You open up an Dingo Crime Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_cheap9') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_cheap9');
	event.player.tell(Text.lightPurple("You open up an Llama Dance Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});

onEvent('item.right_click', event => {
	
  if (event.item.id == 'tfb2:blindbox_everycomp_decent') {
	  
	event.server.runCommandSilent('loot give ' + event.getEntity() + ' loot tfb2:everycomp/everycomp_decent');
	event.player.tell(Text.lightPurple("You open up an Lion Oasis Blind Box!"));
	  
	if (!event.player.isCreativeMode()) { //if creative mode dont consume; not necessary
	  event.item.setCount(event.item.getCount() - 1)
	}
  }
});


